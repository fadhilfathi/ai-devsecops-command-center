{
  "name": "@aicc/contract-tests",
  "version": "0.1.0",
  "private": true,
  "description": "S2.10 + O-3.7 GitOps wire-format contract tests. Cross-validates the JSON Schema in security/wire-format/ against the O-3.7 4-condition auto_actionable gate spec. Owner: GitOpsManager.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
