{
  "name": "@aicc/k8s-health-service",
  "version": "0.1.0",
  "description": "AICC K8s Health Service — pod / namespace / workload / cluster health scoring, issue detection, recommendations",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aicc/models": "workspace:*",
    "@aicc/observability": "workspace:*",
    "@aicc/shared": "workspace:*",
    "fastify": "^5.12.5",
    "pino-pretty": "^13.1.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
