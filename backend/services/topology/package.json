{
  "name": "@aicc/topology-service",
  "version": "0.1.0",
  "description": "AICC Topology Service — service relationships, dependency mapping, namespace relationships, workload relationships (Topology Graph, Application Graph, Service Map)",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aicc/models": "workspace:*",
    "@aicc/observability": "workspace:*",
    "@aicc/shared": "workspace:*",
    "fastify": "^5.12.5",
    "pino-pretty": "^13.1.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}
