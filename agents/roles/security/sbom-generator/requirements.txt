# Runtime dependencies for the SBOM generator.
# Kept in sync with pyproject.toml — pip will use the constraints file
# in production, this is for Docker and direct installs.
fastapi>=0.141.1
uvicorn[standard]>=0.53.0
pydantic>=2.13.5
pydantic-settings>=2.15.0
httpx>=0.28.1
tenacity>=9.1.4
prometheus-client>=0.26.0
structlog>=26.1.0
nats-py>=2.16.0
