# Global styles (legacy)

Global stylesheets in the new AionUi live under
[`frontend/src/styles/`](../../styles/). This directory is preserved for
legacy imports during the migration window (Sprint 2).

- `theme.css` — design tokens (colors, spacing, type scale)
- `reset.css` — modern CSS reset
- `print.css` — print-friendly styles for reports
